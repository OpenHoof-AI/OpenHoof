---
summary: "CLI reference for `openhoof hoofbot` (legacy alias namespace)"
read_when:
  - You maintain older scripts using `openhoof hoofbot ...`
  - You need migration guidance to current commands
title: "hoofbot"
---

# `openhoof hoofbot`

Legacy alias namespace kept for backwards compatibility.

Current supported alias:

- `openhoof hoofbot qr` (same behavior as [`openhoof qr`](/cli/qr))

## Migration

Prefer modern top-level commands directly:

- `openhoof hoofbot qr` -> `openhoof qr`
