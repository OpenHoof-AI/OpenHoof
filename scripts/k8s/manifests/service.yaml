apiVersion: v1
kind: Service
metadata:
  name: openhoof
  labels:
    app: openhoof
spec:
  type: ClusterIP
  selector:
    app: openhoof
  ports:
    - name: gateway
      port: 18789
      targetPort: 18789
      protocol: TCP
