apiVersion: v1
kind: ConfigMap
metadata:
  name: openhoof-config
  labels:
    app: openhoof
data:
  openhoof.json: |
    {
      "gateway": {
        "mode": "local",
        "bind": "loopback",
        "port": 18789,
        "auth": {
          "mode": "token"
        },
        "controlUi": {
          "enabled": true
        }
      },
      "agents": {
        "defaults": {
          "workspace": "~/.openhoof/workspace"
        },
        "list": [
          {
            "id": "default",
            "name": "OpenHoof Assistant",
            "workspace": "~/.openhoof/workspace"
          }
        ]
      },
      "cron": { "enabled": false }
    }
  AGENTS.md: |
    # OpenHoof Assistant

    You are a helpful AI assistant running in Kubernetes.
